const app = require('./app');

// ========== ЗАПУСК СЕРВЕРА ==========
const PORT = process.env.PORT || 5000;
app.listen(PORT, () => {
  console.log(`
  🚀 Сервер запущен!
  📍 Порт: ${PORT}
  🌐 URL: http://localhost:${PORT}
  🛣️  Доступные маршруты:
  GET  /api/test          - Проверка работы
  POST /api/register      - Регистрация
  POST /api/login         - Вход
  GET  /api/profile       - Профиль (требуется авторизация)
  GET  /api/notes         - Все заметки (требуется авторизация)
  GET  /api/notes/:id     - Одна заметка (требуется авторизация)
  POST /api/notes         - Создать заметку (требуется авторизация)
  PUT  /api/notes/:id     - Обновить заметку (требуется авторизация)
  DELETE /api/notes/:id   - Удалить заметку (требуется авторизация)
  GET  /api/stats         - Статистика (требуется авторизация)
  `);
});